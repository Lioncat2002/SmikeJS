{
  "name": "smike",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "babel --presets react main.js --out-dir build && cp ./Smike.js ./build/Smike.js &&  vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^4.4.5"
  },
  "dependencies": {
    "babel-preset-react": "^6.24.1"
  }
}
